// try e catch para tratar erros, ele tenta (try) e se der erro, ele captura (catch) e envia para o usuario, na seguinte estrutura:

/*
try {
    codigo para tentar
} catch (error) {
    codigo para capturar o erro
}
*/

// pode ser usado junto do:

// throw, que seria usado nas funções, na seguinte estrutura, throw('erro no sistema');

// alem disso, pode ser mais especifico, como: throw new Error('erro no sistema');

//o new Error seria um erro de algum tipo dentro do js, tendo varios tipos, depois ver documentação do js.